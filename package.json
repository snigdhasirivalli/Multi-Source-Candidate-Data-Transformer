{
  "name": "multi-source-candidate-data-transformer-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix recruiting-helper && npm run build --prefix recruiting-helper",
    "start": "npm run start:prod --prefix recruiting-helper"
  }
}
